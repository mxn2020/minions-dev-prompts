[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "minions-dev-prompts"
version = "0.2.0"
description = "Reusable prompt templates, prompt chains, and prompt versioning for agent-tasks"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.11"
keywords = ["dev-prompts","ai","minions"]
authors = [
    { name = "Mehdi Nabhani", email = "mehdi@the-mehdi.com" }
]
dependencies = [
    "minions-sdk>=0.2.1",
]

[project.optional-dependencies]
test = ["pytest>=7.0"]

[project.urls]
Homepage = "https://github.com/mxn2020/minions-dev-prompts"
Repository = "https://github.com/mxn2020/minions-dev-prompts"

[tool.hatch.build.targets.wheel]
packages = ["minions_dev_prompts"]

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
